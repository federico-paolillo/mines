info:
  name: Fetch Game
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-url}}/match/{{matchId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
