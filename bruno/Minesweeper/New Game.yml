info:
  name: New Game
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-url}}/match"
  body:
    type: json
    data: |-
      {
        "difficulty": "beginner"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("matchId",res.body.id)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
